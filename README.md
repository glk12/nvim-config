My neovim config for Python Development.

- Neovim (Lua config)
- Lazy.nvim (plugin manager)
- LSP (Language Server Protocol)
- Treesitter
- Telescope
- Autocompletion (nvim-cmp / blink / etc)

 Installation

```bash
git clone git@github.com:glk12/nvim-config.git ~/.config/nvim
nvim
